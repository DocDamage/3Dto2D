{
  "_note": "Placeholder. For real pose-guided WAN, export your ComfyUI pose/control workflow with File -> Export (API), then pass --workflow path/to/exported_api.json. SpriteForge v5 can generate posepacks and patch common LoadImage / image directory inputs, but the exact pose-control nodes depend on your installed ComfyUI custom nodes."
}
